[project]
name = "ansible-loadtest"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "ansible>=13.5.0",
    "boto3>=1.42.81",
]
[dependencies]

